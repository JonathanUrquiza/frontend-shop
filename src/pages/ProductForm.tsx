import React from 'react';

const ProductForm: React.FC = () => {
  return (
    <div className="container mt-4">
      <h2>Formulario de Producto</h2>
      {/* Inputs dinámicos del producto (nombre, precio, etc) */}
    </div>
  );
};

export default ProductForm;
